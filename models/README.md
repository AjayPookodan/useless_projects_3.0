# YOLO Models Directory

This directory stores YOLO model weights if loaded locally.
By default, `ultralytics` automatically downloads `yolov8n.pt` (the nano version, ~6MB, CPU-friendly) on first run when executing `app.py`.

You can also place a custom trained weight file here:
- `yolov8n.pt`
- `yolov8s.pt`
